Data target: domain models + data access for this domain. Common folder structures include: `Model/`, `Mapper/`, `Network/`, `Store/`, `Repository/`, `Container/`.

- IMPORTANT: no SwiftUI here, and no networking/business logic that belongs in a view or composer.

See ios-data-layer skill for rationale + structure.
